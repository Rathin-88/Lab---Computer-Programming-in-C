#include <stdio.h>
int main()
{
int a=2;
int x=35;
int y=47;
int z=87;
float b=10.5;
char c='A';
printf("%d\n",a);
printf("%f\n",b);
printf("%c\n",c);
printf("%o\n",x);
printf("%x\n",y);
printf("%u\n",z);
return 0;
}



// Formatting Specifiers:
------------------------------------------------------------------------
-->  %d is for decimal (normal integer).
-->  %f is for floating-point (decimal numbers). (default: 6 digits after decimal)
-->  %c is for character.
-->  %o is for octal (base 8). (since 35 in decimal = 43 in octal)
-->  %x is for hexadecimal (base 16, lowercase letters). (since 47 in decimal = 2F in hex, but in lowercase)
-->  %u is for unsigned integer. (same as normal, because 87 is positive)
------------------------------------------------------------------------

| Specifier | Meaning                          | Example Value | Output      |
| --------- | -------------------------------- | ------------- | ----------- |
| `%d`      | Decimal integer                  | 2             | `2`         |
| `%f`      | Floating-point number            | 10.5          | `10.500000` |
| `%c`      | Single character                 | 'A'           | `A`         |
| `%o`      | Octal (base 8)                   | 35            | `43`        |
| `%x`      | Hexadecimal (base 16, lowercase) | 47            | `2f`        |
| `%u`      | Unsigned integer                 | 87            | `87`        |

------------------------------------------------------------------------//


